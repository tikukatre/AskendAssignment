import { Routes } from '@angular/router';
import { FilterDashboardComponent } from './filter-dashboard/filter-dashboard.component';

export const routes: Routes = [
  { path: '', component: FilterDashboardComponent },
];
